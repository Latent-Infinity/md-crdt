# Advisory policy for the comparison-only dependency graph.
# Root product crates do not use this file or resolve Yrs.

[advisories]
version = 2
ignore = [
    # Transitive of yrs 0.27.3 (latest on crates.io as of 2026-08-04).
    # No safe upgrade; comparison workspace only (root lockfile has no yrs).
    # Revisit when Yrs drops smallstr or ships a maintained alternative.
    { id = "RUSTSEC-2026-0215", reason = "yrs 0.27.3 depends on unmaintained smallstr; no upgrade path; comparison-only graph" },
]
